# PID Tuning & Configuration

การปรับแต่ง PID (Proportional-Integral-Derivative) เป็นหัวใจสำคัญของความนิ่งและแม่นยำในระบบ NA Platform

## ⚙️ PID Parameters 

| Parameter | Key (NVS) | Default | คำอธิบาย |
|-----------|-----------|---------|---------|
| **Kp**    | `pid_kp`  | 1.2     | **Proportional:** กำหนดความแรงในการตอบสนองตามความผิดพลาด |
| **Ki**    | `pid_ki`  | 0.05    | **Integral:** กำลังที่ใช้สะสมเพื่อแก้ Error ในระยะยาว |
| **Kd**    | `pid_kd`  | 0.4     | **Derivative:** ตัวหน่วงเพื่อลดการสั่นสะเทือน (Overshoot) |

## 🕹️ Joystick & Deadzone

การตั้งค่าเพื่อป้องกันการขยับเองของจอยสติ๊ก (Stick Drift):

*   **Deadzone:** กำหนดเป็น % ของระยะจอยสติ๊กทั้งหมด (ค่าเริ่มต้น 5%)
*   **Calibration:** ระบบจะบันทึกค่า Min, Center, Max ของจอยแต่ละตัวเพื่อให้ได้ Linear Output ที่ดีที่สุด

## 🛡️ Security Settings

| Setting | Key | คำอธิบาย |
|---------|-----|---------|
| **Encryption** | `sec_enc` | เปิด/ปิด การเข้ารหัส AES-256 (แนะนำ: ON) |
| **HMAC Auth** | `sec_hmac`| เปิด/ปิด การตรวจสอบความถูกต้องของข้อมูล (แนะนำ: ON) |
| **Rate Limit** | `sec_rl_cps`| จำกัดความเร็วคำสั่ง (Default: 100 cmds/sec) |

---
> [!IMPORTANT]
> การปรับค่า PID ที่สูงเกินไปอาจทำให้มอเตอร์สั่นสะเทือนรุนแรงและเกิดความร้อนสะสม ควรปรับเพิ่มทีละ 0.1 เท่านั้น
